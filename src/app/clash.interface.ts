export interface Scores {
    name: string;
    class: string;
    score: string;
}

export interface PlayerDetails {
    playerName: string;
    playerClass: string
    selectedClassIndex: number;
  }
